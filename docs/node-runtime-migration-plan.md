# Node Runtime Migration Plan

Migration is complete.

Current state:

- runtime is Node-only
- public surface is `bin/octopus.js`
- lifecycle commands and gates load `src/**/*.ts`
- legacy shell lifecycle scripts and gate wrappers were removed

This document is kept only as an archive marker so old references do not imply an active mixed-runtime plan.
